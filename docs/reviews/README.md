# Completed pair reviews

Filled-in review sheets go here, one file per reviewer, named
`<seed>-<reviewer>.md`. Record the seed used, since the X/Y mapping is
recoverable only from it.

**Empty so far.** The procedure is in [../PAIR-REVIEW.md](../PAIR-REVIEW.md).
